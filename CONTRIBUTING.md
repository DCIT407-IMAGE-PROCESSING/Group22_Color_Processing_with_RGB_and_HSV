# Contributing

Please follow these simple rules:
- Work in feature branches and open a PR to merge into `main`.
- Keep `notebooks/main_project.ipynb` focused; perform individual experiments in `notebooks/explorations/`.
- Add tests or runnable examples in `src/` when adding functionality.
